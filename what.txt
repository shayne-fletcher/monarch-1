

>>> Lint for fbcode/monarch/monarch_tensor_worker/src/borrow.rs:

   Error  (RUSTFIX) E0599
    the function or associated item `new` exists for struct
    `torch_sys2::Device`, but its trait bounds were not satisfied
    the following trait bounds were not satisfied:
    `torch_sys2::Device: hyperactor::Actor`
    which is required by `torch_sys2::Device: hyperactor::RemoteSpawn`
    `torch_sys2::Device: hyperactor::actor::Referable`
    which is required by `torch_sys2::Device: hyperactor::RemoteSpawn`
    `torch_sys2::Device: hyperactor::actor::Binds<torch_sys2::Device>`
    which is required by `torch_sys2::Device: hyperactor::RemoteSpawn`
    `torch_sys2::Device: std::default::Default`
    which is required by `torch_sys2::Device: hyperactor::RemoteSpawn`
    `&torch_sys2::Device: hyperactor::Actor`
    which is required by `&torch_sys2::Device: hyperactor::RemoteSpawn`
    `&torch_sys2::Device: hyperactor::actor::Referable`
    which is required by `&torch_sys2::Device: hyperactor::RemoteSpawn`
    `&torch_sys2::Device: hyperactor::actor::Binds<&torch_sys2::Device>`
    which is required by `&torch_sys2::Device: hyperactor::RemoteSpawn`
    `&torch_sys2::Device: std::default::Default`
    which is required by `&torch_sys2::Device: hyperactor::RemoteSpawn`
    `&mut torch_sys2::Device: hyperactor::Actor`
    which is required by `&mut torch_sys2::Device: hyperactor::RemoteSpawn`
    `&mut torch_sys2::Device: hyperactor::actor::Referable`
    which is required by `&mut torch_sys2::Device: hyperactor::RemoteSpawn`
    `&mut torch_sys2::Device: hyperactor::actor::Binds<&mut
    torch_sys2::Device>`
    which is required by `&mut torch_sys2::Device: hyperactor::RemoteSpawn`
    `&mut torch_sys2::Device: std::default::Default`
    which is required by `&mut torch_sys2::Device: hyperactor::RemoteSpawn`

             337 
             338     #[async_timed_test(timeout_secs = 60)]
             339     async fn borrow_cpu() -> Result<()> {
    >>>      340         basic_borrow_test_impl(Device::new(DeviceType::CPU)).await
             341     }
             342 
             343     #[async_timed_test(timeout_secs = 60)]

   Error  (RUSTFIX) E0599
    the function or associated item `new` exists for struct
    `torch_sys2::Device`, but its trait bounds were not satisfied
    the following trait bounds were not satisfied:
    `torch_sys2::Device: hyperactor::Actor`
    which is required by `torch_sys2::Device: hyperactor::RemoteSpawn`
    `torch_sys2::Device: hyperactor::actor::Referable`
    which is required by `torch_sys2::Device: hyperactor::RemoteSpawn`
    `torch_sys2::Device: hyperactor::actor::Binds<torch_sys2::Device>`
    which is required by `torch_sys2::Device: hyperactor::RemoteSpawn`
    `torch_sys2::Device: std::default::Default`
    which is required by `torch_sys2::Device: hyperactor::RemoteSpawn`
    `&torch_sys2::Device: hyperactor::Actor`
    which is required by `&torch_sys2::Device: hyperactor::RemoteSpawn`
    `&torch_sys2::Device: hyperactor::actor::Referable`
    which is required by `&torch_sys2::Device: hyperactor::RemoteSpawn`
    `&torch_sys2::Device: hyperactor::actor::Binds<&torch_sys2::Device>`
    which is required by `&torch_sys2::Device: hyperactor::RemoteSpawn`
    `&torch_sys2::Device: std::default::Default`
    which is required by `&torch_sys2::Device: hyperactor::RemoteSpawn`
    `&mut torch_sys2::Device: hyperactor::Actor`
    which is required by `&mut torch_sys2::Device: hyperactor::RemoteSpawn`
    `&mut torch_sys2::Device: hyperactor::actor::Referable`
    which is required by `&mut torch_sys2::Device: hyperactor::RemoteSpawn`
    `&mut torch_sys2::Device: hyperactor::actor::Binds<&mut
    torch_sys2::Device>`
    which is required by `&mut torch_sys2::Device: hyperactor::RemoteSpawn`
    `&mut torch_sys2::Device: std::default::Default`
    which is required by `&mut torch_sys2::Device: hyperactor::RemoteSpawn`

             342 
             343     #[async_timed_test(timeout_secs = 60)]
             344     async fn borrow_cuda() -> Result<()> {
    >>>      345         basic_borrow_test_impl(Device::new(DeviceType::CUDA)).await
             346     }
             347 
             348     #[async_timed_test(timeout_secs = 60)]


>>> Lint for fbcode/monarch/monarch_tensor_worker/src/comm.rs:

   Error  (RUSTFIX) E0277
    `?` couldn't convert the error: `std::string::String: derive_more::Error`
    is not satisfied
    the question mark operation (`?`) implicitly performs a conversion on the
    error value using the `From` trait
    required for `anyhow::Error` to implement
    `derive_more::From<std::string::String>`

             634         res1?;
             635 
             636         // Only dest_rank=0 should have the reduced value.
    >>>      637         assert!(allclose(
             638             &cell0.borrow(),
             639             &factory_float_tensor(&[3.0], device0.into())
             640         )?);
             641         // Non-dest ranks should have the original value.
             642         assert!(allclose(
             643             &cell1.borrow(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

             696                     vec![WireValue::IntList(vec![2, 3])],
             697                     HashMap::from([(
             698                         "device".into(),
    >>>      699                         WireValue::Device("cuda".try_into().unwrap()),
             700                     )]),
             701                 )
             702                 .unwrap(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

             711                     size: vec![2, 3],
             712                     dtype: ScalarType::Float,
             713                     layout: Layout::Strided,
    >>>      714                     device: "cuda".try_into().unwrap(),
             715                 },
             716                 mesh: 1.into(),
             717                 stream: 0.into(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

             730                     vec![WireValue::IntList(vec![2, 3]), WireValue::Double(2.0)],
             731                     HashMap::from([(
             732                         "device".into(),
    >>>      733                         WireValue::Device("cuda".try_into().unwrap()),
             734                     )]),
             735                 )
             736                 .unwrap(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

             764                     size: vec![2, 3],
             765                     dtype: ScalarType::Float,
             766                     layout: Layout::Strided,
    >>>      767                     device: "cuda".try_into()?,
             768                 },
             769                 mesh: 1.into(),
             770                 stream: 0.into(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

             781                 function: "torch.ops.aten.full.default".into(),
             782                 args_kwargs: ArgsKwargs::from_wire_values(
             783                     vec![WireValue::IntList(vec![2, 3]), WireValue::Double(4.0)],
    >>>      784                     HashMap::from([("device".into(), WireValue::Device("cuda".try_into()?))]),
             785                 )
             786                 .unwrap(),
             787                 stream: 0.into(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

             893                             vec![WireValue::IntList(vec![2, 3]), WireValue::Double(2.0)],
             894                             HashMap::from([(
             895                                 "device".into(),
    >>>      896                                 WireValue::Device("cuda".try_into().unwrap()),
             897                             )]),
             898                         )
             899                         .unwrap(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

             913                             size: vec![2, 3],
             914                             dtype: ScalarType::Float,
             915                             layout: Layout::Strided,
    >>>      916                             device: "cuda".try_into().unwrap(),
             917                         },
             918                         from_stream: 0.into(),
             919                         to_stream: 0.into(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

             945                             size: vec![2, 3],
             946                             dtype: ScalarType::Float,
             947                             layout: Layout::Strided,
    >>>      948                             device: "cuda".try_into().unwrap(),
             949                         },
             950                         from_stream: 0.into(),
             951                         to_stream: 0.into(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

             968                             vec![WireValue::IntList(vec![2, 3]), WireValue::Double(2.0)],
             969                             HashMap::from([(
             970                                 "device".into(),
    >>>      971                                 WireValue::Device("cuda".try_into().unwrap()),
             972                             )]),
             973                         )
             974                         .unwrap(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

            1058                             vec![WireValue::IntList(vec![2, 3]), WireValue::Double(2.0)],
            1059                             HashMap::from([(
            1060                                 "device".into(),
    >>>     1061                                 WireValue::Device("cuda".try_into().unwrap()),
            1062                             )]),
            1063                         )
            1064                         .unwrap(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

            1074                             vec![WireValue::IntList(vec![2, 3]), WireValue::Double(4.0)],
            1075                             HashMap::from([(
            1076                                 "device".into(),
    >>>     1077                                 WireValue::Device("cuda".try_into().unwrap()),
            1078                             )]),
            1079                         )
            1080                         .unwrap(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

            1095                             size: vec![2, 3],
            1096                             dtype: ScalarType::Float,
            1097                             layout: Layout::Strided,
    >>>     1098                             device: "cuda".try_into().unwrap(),
            1099                         },
            1100                         from_stream: 0.into(),
            1101                         to_stream: 0.into(),

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

            1109                             vec![WireValue::IntList(vec![2, 3]), WireValue::Double(2.0)],
            1110                             HashMap::from([(
            1111                                 "device".into(),
    >>>     1112                                 WireValue::Device("cuda".try_into().unwrap()),
            1113                             )]),
            1114                         )
            1115                         .unwrap(),

   Error  (RUSTFIX) E0433
    failed to resolve: use of undeclared type `CommWork`

            1204             port1.0,
            1205         ))?;
            1206         let (work0, work1) = tokio::try_join!(
    >>>     1207             CommWork::from(vec![cell0.clone()], port0.1),
            1208             CommWork::from(vec![cell1.clone()], port1.1)
            1209         )
            1210         .unwrap();

   Error  (RUSTFIX) E0433
    failed to resolve: use of undeclared type `CommWork`

            1205         ))?;
            1206         let (work0, work1) = tokio::try_join!(
            1207             CommWork::from(vec![cell0.clone()], port0.1),
    >>>     1208             CommWork::from(vec![cell1.clone()], port1.1)
            1209         )
            1210         .unwrap();
            1211         // Wait for the work to enqueue onto the stream.


>>> Lint for fbcode/monarch/monarch_tensor_worker/src/stream.rs:

   Warning  (RUSTFIX) dead_code
    field `world_size` is never read
    `StreamActor` has a derived impl for the trait `Debug`, but this is
    intentionally ignored during dead code analysis
    `#[warn(dead_code)]` (part of `#[warn(unused)]`) on by default

             416 /// For CPU operators, streams will just execute synchronously on their own OS
             417 /// thread.
             418 #[derive(Debug)]
    >>>      419 pub struct StreamActor {
             420     world_size: usize,
             421     rank: usize,
             422     /// Mapping of refs in the controller environment to TensorIndex in this

   Warning  (RUSTFIX) unused_variables
    unused variable: `cx`
    `#[warn(unused_variables)]` (part of `#[warn(unused)]`) on by default
    if this is intentional, prefix it with an underscore

             720     fn call_python_fn<'py>(
             721         &mut self,
             722         py: Python<'py>,
    >>>      723         cx: &Context<Self>,
             724         function: Option<ResolvableFunction>,
             725         args_kwargs: ArgsKwargs,
             726         _mutates: &[Ref],

   Warning  (RUSTFIX) unused_variables
    unused variable: `mesh`
    if this is intentional, prefix it with an underscore

             747 
             748         let remote_process_groups = remote_process_groups
             749             .into_iter()
    >>>      750             .map(|(gref, (mesh, dims, comm))| {
             751                 let group = match self.remote_process_groups.entry(gref) {
             752                     Entry::Occupied(ent) => ent.get().clone_ref(py),
             753                     Entry::Vacant(ent) => {

   Warning  (RUSTFIX) unused_variables
    unused variable: `dims`
    if this is intentional, prefix it with an underscore

             747 
             748         let remote_process_groups = remote_process_groups
             749             .into_iter()
    >>>      750             .map(|(gref, (mesh, dims, comm))| {
             751                 let group = match self.remote_process_groups.entry(gref) {
             752                     Entry::Occupied(ent) => ent.get().clone_ref(py),
             753                     Entry::Vacant(ent) => {

   Warning  (RUSTFIX) unused_variables
    unused variable: `comm`
    if this is intentional, prefix it with an underscore

             747 
             748         let remote_process_groups = remote_process_groups
             749             .into_iter()
    >>>      750             .map(|(gref, (mesh, dims, comm))| {
             751                 let group = match self.remote_process_groups.entry(gref) {
             752                     Entry::Occupied(ent) => ent.get().clone_ref(py),
             753                     Entry::Vacant(ent) => {

   Warning  (RUSTFIX) unused_variables
    unused variable: `ent`
    if this is intentional, prefix it with an underscore

             750             .map(|(gref, (mesh, dims, comm))| {
             751                 let group = match self.remote_process_groups.entry(gref) {
             752                     Entry::Occupied(ent) => ent.get().clone_ref(py),
    >>>      753                     Entry::Vacant(ent) => {
             754                         panic!("no longer implemented");
             755                     }
             756                 };

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

            2003         }
            2004 
            2005         async fn set_tensor(&mut self, reference: Ref, data: &[f32]) -> Result<()> {
    >>>     2006             let tensor = TensorCell::new(factory_float_tensor(data, "cuda".try_into().unwrap()));
            2007             self.stream_actor
            2008                 .set_tensor_ref_unit_tests_only(&self.client, reference, Ok(tensor))
            2009                 .await

   Error  (RUSTFIX) E0277
    the trait bound `torch_sys2::Device: derive_more::TryFrom<&str>` is not
    satisfied
    the trait `From<&str>` is not implemented for `torch_sys2::Device`
    but trait `From<torch_sys2::CudaDevice>` is implemented for it
    for that trait implementation, expected `torch_sys2::CudaDevice`, found
    `&str`
    required for `&str` to implement `derive_more::Into<torch_sys2::Device>`
    required for `torch_sys2::Device` to implement
    `derive_more::TryFrom<&str>`
    required for `&str` to implement `derive_more::TryInto<torch_sys2::Device>`

            2019                 .unwrap();
            2020 
            2021             let result = allclose(
    >>>     2022                 &factory_float_tensor(data, "cpu".try_into().unwrap()),
            2023                 &actual.borrow(),
            2024             )
            2025             .unwrap();


>>> Lint for fbcode/monarch/python/monarch/safe_torch.py:

   Warning  (PYREMODE) missing-pyre-mode-header
    Your file is type checked by Pyre but is missing a Pyre mode header (e.g.
    `# pyre-strict`, `# pyre-unsafe`, or `# pyre-ignore-all-errors`) at the
    top of the file (after any shebang or licenses). Please add a header to
    guarantee type checker accuracy.


>>> Lint for fbcode/monarch/torch-sys2/src/lib.rs:

   Warning  (RUSTFIX) non_camel_case_types
    variant `Float8_e5m2` should have an upper camel case name
    `#[warn(non_camel_case_types)]` (part of `#[warn(nonstandard_style)]`) on
    by default
    convert the identifier to upper camel case

             308     Bits4x2 = 20,
             309     Bits8 = 21,
             310     Bits16 = 22,
    >>>      311     Float8_e5m2 = 23,
             312     Float8_e4m3fn = 24,
             313     Float8_e5m2fnuz = 25,
             314     Float8_e4m3fnuz = 26,

   Warning  (RUSTFIX) non_camel_case_types
    variant `Float8_e4m3fn` should have an upper camel case name
    convert the identifier to upper camel case

             309     Bits8 = 21,
             310     Bits16 = 22,
             311     Float8_e5m2 = 23,
    >>>      312     Float8_e4m3fn = 24,
             313     Float8_e5m2fnuz = 25,
             314     Float8_e4m3fnuz = 26,
             315 }

   Warning  (RUSTFIX) non_camel_case_types
    variant `Float8_e5m2fnuz` should have an upper camel case name
    convert the identifier to upper camel case

             310     Bits16 = 22,
             311     Float8_e5m2 = 23,
             312     Float8_e4m3fn = 24,
    >>>      313     Float8_e5m2fnuz = 25,
             314     Float8_e4m3fnuz = 26,
             315 }
             316 

   Warning  (RUSTFIX) non_camel_case_types
    variant `Float8_e4m3fnuz` should have an upper camel case name
    convert the identifier to upper camel case

             311     Float8_e5m2 = 23,
             312     Float8_e4m3fn = 24,
             313     Float8_e5m2fnuz = 25,
    >>>      314     Float8_e4m3fnuz = 26,
             315 }
             316 
             317 /// Remote serde implementation for ScalarType

   Warning  (RUSTFIX) non_camel_case_types
    variant `Float8_e5m2` should have an upper camel case name
    convert the identifier to upper camel case

             341     Bits4x2,
             342     Bits8,
             343     Bits16,
    >>>      344     Float8_e5m2,
             345     Float8_e4m3fn,
             346     Float8_e5m2fnuz,
             347     Float8_e4m3fnuz,

   Warning  (RUSTFIX) non_camel_case_types
    variant `Float8_e4m3fn` should have an upper camel case name
    convert the identifier to upper camel case

             342     Bits8,
             343     Bits16,
             344     Float8_e5m2,
    >>>      345     Float8_e4m3fn,
             346     Float8_e5m2fnuz,
             347     Float8_e4m3fnuz,
             348 }

   Warning  (RUSTFIX) non_camel_case_types
    variant `Float8_e5m2fnuz` should have an upper camel case name
    convert the identifier to upper camel case

             343     Bits16,
             344     Float8_e5m2,
             345     Float8_e4m3fn,
    >>>      346     Float8_e5m2fnuz,
             347     Float8_e4m3fnuz,
             348 }
             349 

   Warning  (RUSTFIX) non_camel_case_types
    variant `Float8_e4m3fnuz` should have an upper camel case name
    convert the identifier to upper camel case

             344     Float8_e5m2,
             345     Float8_e4m3fn,
             346     Float8_e5m2fnuz,
    >>>      347     Float8_e4m3fnuz,
             348 }
             349 
             350 impl FromPyObject<'_> for ScalarType {

   Error  (RUSTFIX) clippy::undocumented_unsafe_blocks
    unsafe block missing a safety comment
    consider adding a safety comment on the preceding line
    for further information visit https://rust-lang.github.io/rust-clippy/
    rust-1.91.0/index.html#undocumented_unsafe_blocks
    requested on the command line with `-D clippy::undocumented-unsafe-blocks`

             575 
             576     pub fn borrow(&self) -> BorrowGuard {
             577         BorrowGuard {
    >>>      578             tensor: unsafe { self.tensor.clone_unsafe() },
             579         }
             580     }
             581 

   Error  (RUSTFIX) clippy::undocumented_unsafe_blocks
    unsafe block missing a safety comment
    consider adding a safety comment on the preceding line
    for further information visit https://rust-lang.github.io/rust-clippy/
    rust-1.91.0/index.html#undocumented_unsafe_blocks

             581 
             582     pub fn borrow_mut(&self) -> BorrowGuardMut {
             583         BorrowGuardMut {
    >>>      584             tensor: unsafe { self.tensor.clone_unsafe() },
             585         }
             586     }
             587 
