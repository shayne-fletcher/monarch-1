# @generated by autocargo from //monarch/hyperactor_telemetry/tester:tester

[package]
name = "tester"
version = "0.0.0"
authors = ["Meta"]
edition = "2024"
license = "BSD-3-Clause"

[[bin]]
name = "tester"
path = "main.rs"

[dependencies]
hyperactor_telemetry = { version = "0.0.0", path = ".." }
opentelemetry = "0.29"
tracing = { version = "0.1.41", features = ["attributes", "valuable"] }

[lints]
rust = { unexpected_cfgs = { check-cfg = ["cfg(fbcode_build)"], level = "warn" } }
